



<%#<% @passenger.each do |passenger|%>

 <%# <p>Name: <%= link_to passenger.name, passenger_path(passenger) %> <%#Phone Number: <%=passenger.phone_num%> 

 <%# <%= button_to "Edit", edit_passenger_path(passenger), method: :get %>

 <%# <%= button_to "Delete", passenger, method: :delete, data: {confirm: "Are you sure you want to delete this passenger?"} %>

 <%# <%end %> 






  <table>
    <tr>
      <th>Name</th>
      <th>Phone Number</th>
    </tr>

  <% @passenger.each do |passenger|%>
    <tr>
      <td><%= link_to passenger.name, passenger_path(passenger) %> </td>
      <td><%= passenger.phone_num %></td>
    </tr>
    <%end %>
</table>


